
// This file is kept as a placeholder but the actual logic has been moved to the Flask backend
// This prevents breaking any imports in the React codebase

export const generatePoemFromWord = (word: string): string[] => {
  console.warn("This function is deprecated. Using Flask API instead.");
  return [
    "This poem was not generated.",
    "The functionality has been moved",
    "to a Flask backend for better",
    "Python-based soft computing."
  ];
};
